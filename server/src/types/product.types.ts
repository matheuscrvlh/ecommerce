export type ProductBody = {
    marca_id?: number
    nome: string
    slug: string
    descricao?: string
    descricao_curta?: string
    tipo_produto: string
    ativo?: boolean
    publicado?: boolean
}